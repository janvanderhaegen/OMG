# OMG - Open Modular Gardening – Session Log

- Date: 2026-03-02
- Duration: N/A
- Related user stories: N/A

## Context
Optimized read performance for garden and plant management endpoints using DbContext raw SQL and narrowed column selection, then finalized and pushed the changes (commit 33565b39485db96873766cbfaf33802d8443a5b1 on main).

## What was done
- Switched management garden GET endpoints to use ManagementDbContext and raw SQL projections directly into `GardenResponse`.
- Switched management plant GET endpoints to use ManagementDbContext with raw SQL projections into `PlantResponse`, ensuring garden ownership checks remain enforced.
- Ensured write operations (create/update/delete) continue to use the existing repository and unit-of-work abstractions.
- Committed the changes with a perf-focused message and pushed to `origin/main`.

## Design decisions
- Chose EF Core `SqlQueryRaw` into response DTOs to avoid materializing full entities and to limit columns to exactly what the API returns.
- Kept authorization and ownership checks in place by filtering on `UserId` and `Deleted` flags at the SQL level before returning data.

